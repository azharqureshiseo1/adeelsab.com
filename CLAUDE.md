# CLAUDE.md — AdeelSab.com

Handoff context for any Claude Code session working on this repo. Read this first, then
`README.md` (architecture), `CONTENT-TODO.md` (open values) and `DEPLOY.md` (Hostinger).

## What this is

Corporate + merchant-acquisition website for **AdeelSab** (ADEELSAB (PRIVATE) LIMITED), a
Pakistani multi-vendor marketplace that is built but **not launched**. The site collects sellers
onto a **pre-launch waitlist**. It is NOT the marketplace (no catalogue/cart/checkout).

- Pakistan only. English + Urdu (RTL, Noto Nastaliq Urdu). No dark mode, no other countries/currencies.
- Audience priority (do not reorder): **local sellers** (onboarding) → **resellers** (waitlist only,
  catalogue not ready) → **dropshippers** (Phase 2 stub).
- Deploy target: **Hostinger Business, Node.js 22, server-mode Next.js from GitHub**. No `output: 'export'`.

## Commands

```bash
npm ci
npm run dev            # http://localhost:3000
npm run build          # every page must be ○/●; only /api/waitlist may be ƒ
npm run lint && npm run typecheck
npm run brand:assets   # rebuild public/brand, public/images, public/logos, icons, OG from assets/
```

## Non-negotiable rules (from the original brief)

1. Every `page.tsx` exports `dynamic = "force-static"`. Never use `cookies()`/`headers()`/no-store fetches in pages.
2. No runtime writes to disk. Leads go to Supabase (`app/api/waitlist/route.ts`, `supabase/schema.sql`).
3. All user-facing copy lives in `content/site.ts` as `{ en, ur }`. Render with `<T v={…} />`.
4. **Never invent numbers** (users, GMV, testimonials, rates). Unconfirmed values live in
   `content/data/facts.ts` as `todo("…")` and render as visible orange `TODO:` markers.
5. Never soften or hide RTO charges, commission or payout timelines.
6. Ask the owner before adding any npm package outside: next, react, tailwind v4, lucide-react,
   @supabase/supabase-js, nodemailer, @next/mdx (+ its loader/react/types), eslint, prettier.
7. Orange is an accent only; max 3 `--brand-gradient` uses per page. Header = dark logo, dark sections = orange logo.

## Decisions made with the owner (chronological)

- **Build**: all 16 steps of the brief done — home (10 sections), /founding-seller, /sell (+ local-sellers,
  resellers with margin calculator, dropshippers), /delivery, /pricing, /payouts, /reseller-listings,
  /how-it-works, /seller-hub (7 MDX guides EN+UR), /blog (3 posts EN+UR), /about, /careers, /contact,
  /legal/* (draft, EN+UR), 404, SEO/JSON-LD/sitemap/robots, PR-only CI.
- **Urdu** was written alongside English (not left as TODO). Needs native review before launch.
- Added colour token **`brand-700` (#C23400)** for small orange text (AA contrast), and later
  **`brand-650` (#D63800)** for primary buttons: white on `brand-500` is only 3.31:1, which failed
  Lighthouse. Buttons are now `brand-650` (4.75:1) with `--brand-gradient-strong` on hover (every
  stop ≥4.75:1). `--brand-gradient` is decorative only — its light end is 2.3:1 on white, so
  `.gradient-text` uses `--brand-gradient-text` instead. `text-ink-400` is for dark grounds only
  (3.64:1 on white); use `ink-500` for small grey text on white.
- **Brand assets**: owner supplied real logos, AdeelSab Couriers logos, bag mark, 5 photos and logos for
  TCS, Leopards, PostEx, M&P, JazzCash, Easypaisa, Stripe, SECP, FBR. Originals in `assets/`; web copies
  generated by `npm run brand:assets`.
- **Company details** (`content/data/facts.ts`):
  - Registered name: **ADEELSAB (PRIVATE) LIMITED**
  - SECP Reg. No. **0353167** · FBR Reg. No. **J816970**
  - Address: **Office No 20, First floor, Takbeer Plaza, Al Faisal Town, Lahore, Pakistan**
  - Email: **support@adeelsab.com** (also used for business and privacy requests)
  - WhatsApp support: **+92 325 0040009** (`NEXT_PUBLIC_WHATSAPP=923250040009`)
  - Landline (**sellers/vendors only**, not a customer line): **042 3663 2828**
  - Careers: **careers@adeelsab.com** · Office hours: **9:00 am – 9:00 pm (PKT)**
  - Social: facebook.com/adeelsab.pk, instagram.com/adeelsab.pk, tiktok.com/@adeelsab.pk, threads.com/@adeelsab.pk
- **Footer**: "© 2026 AdeelSab. All rights reserved." + "Developed by Refine Core Private Limited"
  linking to https://refinecore.pk/. Shows SECP/FBR, delivery-partner and payment logo chips.
- **Founder**: owner does NOT want a founder photo/name/box anywhere. Trust bar and /about show the
  registered company instead. Don't add it back.
- **Payouts: weekly** ("every week" / "ہر ہفتے" written into copy; there is no payout-days token).
- **Delivery charge: flat Rs 350 per parcel**, every city (`facts.deliveryCharge`, `shipping.rateCard`,
  packaging guide). Volumetric-weight copy was removed.
- **Own fleet (AdeelSab Couriers): Lahore only** (`content/data/cities.ts` → `ownFleet: true`).
- **Commission: 7% flat**, every category AND every seller at launch, Founding Sellers included
  (`content/data/categories.ts`). The Founding Seller programme carries **no commission discount** —
  `foundingCommissionRate` and `foundingCommissionMonths` were removed, along with the commission
  benefit card, the home benefit line and the "after the founding period" FAQ.
- **No listing fee, no COD handling fee, no payout transfer fee, no dispute hold period** — commission
  is the only fee on a sale. Those rows/facts were removed rather than set to "Rs 0" where the owner
  asked for removal.
- **RTO: Rs 300 per returned parcel, paid by the SELLER**, the same in every zone
  (`facts.rtoCharge`, `shipping.rtoCharges`). A customer who refuses a parcel is **not** billed for
  the return. (This reverses an earlier decision that the customer paid it.) Never soften or hide
  this — rule 5.
- **Customer returns: 2-day window, return shipping paid by the customer.**
- **Minimum payout Rs 3,000 · COD remitted 9 days after delivery.**
- **Founding Seller: first 100 only.** Benefits are free listing creation, priority WhatsApp support,
  featured placement at launch and the badge — **not** a commission discount. No registered counter
  on the page; it states the cap instead.
- **No launch date is published** anywhere; Founding Sellers are told first, on WhatsApp.
- **Resellers are now "Coming soon"**, like dropshippers (badge, nav, compare table, meta).
- **3 delivery attempts before RTO.** Delivery times: Lahore same/next day, major cities 2–3 working
  days, rest of Pakistan 3–4 working days (`shipping.ts` → `timelines`, now bilingual `L`).
- **`/llms.txt`** (`app/llms.txt/route.ts`) describes the site for AI agents, generated at build time
  from the same route list as the sitemap. Keep its key-facts paragraph in sync with `facts.ts`.
- **Courier partners: TCS, PostEx, TRAX** (home stat says "3"). Leopards and M&P were dropped by the
  owner; their logo files are deleted. TRAX's logo is in `assets/logos/trax.png`.

## Open questions for the owner

- Minimum payout amount; RTO charge (NOT assumed to be Rs 350); COD remittance days; whether heavy/bulky
  parcels cost more than Rs 350.
- Commission % per category; Founding Seller cap / reduced rate / months / registered count; launch date.
- Real WhatsApp number; office hours; careers email; mobile app at launch?
- Confirm Stripe usage (Stripe doesn't onboard Pakistan-registered businesses directly) and M&P partnership.
- Legal: jurisdiction city, privacy retention/response time, full Seller Agreement text, lawyer review.
- Full list with file locations: `CONTENT-TODO.md`.

## Working notes

- Owner communicates in Roman Urdu; reply in Roman Urdu, keep technical terms/paths in English.
- Git: branch `main`, no remote configured yet. Commit after each change; don't push unless asked.
- `site.ts` must never be imported by client components (keeps copy out of JS bundles). Client islands
  receive rendered copy as props (see `HeaderIslands.tsx`, `WaitlistForm.tsx`).
